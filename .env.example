# Atlassian API token: https://id.atlassian.com/manage-profile/security/api-tokens
ATLASSIAN_EMAIL=you@lotusflare.com
ATLASSIAN_API_TOKEN=your_api_token_here
ATLASSIAN_SITE=lotusflare.atlassian.net

# Zoho People (optional — for employee / department mapping)
# Create Server-based app: https://api-console.zoho.com/
# Redirect URI: http://localhost:8080/oauth/callback
# Run: npm run zoho:auth
ZOHO_CLIENT_ID=
ZOHO_CLIENT_SECRET=
ZOHO_REFRESH_TOKEN=
ZOHO_ACCOUNTS_URL=https://accounts.zoho.com
ZOHO_PEOPLE_URL=https://people.zoho.com

# Site password (optional locally — hash only, not the plain password)
# Generate: npm run access:hash -- your-password
# VITE_ACCESS_PASSWORD_HASH=

# Slack bot reminders (Cloudflare Worker) — see DEPLOY.md "Slack bot DMs"
# Worker URL (no trailing slash). Can also set remindApiUrl in public/config/slack.json
# VITE_REMIND_API_URL=https://lf-confluence-slack-remind.<account>.workers.dev
# Shared key (must match Worker secret REMIND_API_KEY). Never commit the real value.
# VITE_REMIND_API_KEY=
